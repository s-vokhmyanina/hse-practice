package libact4e.impl;

import libact4e.FiniteMap;
import libact4e.FiniteRelation;

public class FiniteMapOperations {

    public <K, T> FiniteMap<K, T> compose(FiniteMap<K, T> f, FiniteMap<K, T> g) {
        return null;
    }

    public <K, T> FiniteRelation<K, T> asRelation(FiniteMap<K, T> f) {
        return null;
    }
}
