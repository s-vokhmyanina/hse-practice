package libact4e.impl;

import libact4e.FinitePoset;
import libact4e.FiniteSet;

public class ArrayFinitePoset<T> implements FinitePoset<T> {
    @Override
    public FiniteSet<T> carrier() {
        return null;
    }
}
