package libact4e;

import libact4e.utils.UnderDevelopment;

@UnderDevelopment
public interface FiniteSetDisjointUnion<C, E> {
    //Specialization of SetDisjointUnion where we deal with FiniteSets.
}
